import React from 'react';

type OscarProps = {
    children: React.ReactNode
};

const Oscar = (porps: OscarProps) => {
  return (
    <div>
      <p>{props.children}</p>
    </div>
  )
}

export default Oscar;
